#ifndef INVALID_MAPPING_REASON_HPP
#define INVALID_MAPPING_REASON_HPP
enum class EnumInvalidMappingReason {
    INVALID_PLACEMENT = 0,
    INVALID_ROUTING = 1,
    INVALID_SCHEDULING = 2,
    NONE = 3
};
#endif